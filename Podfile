# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MetaWeatherMusala' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod "Alamofire"
  pod "RealmSwift"
  pod "SwiftyJSON"
  pod 'SDWebImage', '~> 5.0'

  target 'MetaWeatherMusalaTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
